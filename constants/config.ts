/**
 * API base URL.
 * Change to your production URL when deploying.
 * For Android emulator, use 10.0.2.2 instead of localhost.
 * For a physical device on the same WiFi, use your computer's local IP.
 */

// Android emulator: 10.0.2.2 | Real device on WiFi: 192.168.x.x | Deployed: https://your-domain.com
export const API_BASE_URL = 'http://10.0.2.2:3002'

// App config
export const APP_NAME = 'Amazonia'
export const CURRENCY = 'Rs.'   // Change to $ for USD
export const OTP_LENGTH = 6
export const OTP_EXPIRY_SECONDS = 300  // 5 minutes
